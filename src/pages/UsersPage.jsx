import { cn } from '@/lib/utils';

export default function UsersPage() {
  return (
    <div className="p-6">
      <h1 className="text-base font-semibold mb-1">Users</h1>
      <p className="text-xs text-muted-foreground">Manage staff and admin accounts.</p>
      <div className="mt-8 flex items-center justify-center h-40 border rounded-lg border-dashed text-muted-foreground text-sm">
        User management coming next
      </div>
    </div>
  );
}
